# AI usage

Zenbit builds and maintains this repository, and uses **Claude Code** (Anthropic) to draft prose and code against the specifications in [`docs/spec/`](./docs/spec/), which are written before the code rather than derived from it. **The UI and UX design, the technical architecture, the integration with the existing Xovi codebase, and the video, voice narration and presentation deck are Zenbit's, with no model output in them.** The same holds for the invariant that an agent may propose and may never confirm, the decision that an agent earns no credit, the scope cuts, the evaluation numbers quoted in the README (164 frames from 13 verified clips, measured 2026-08-28: 159 correct station reads, 50 correct and 0 incorrect at frame level with 114 referred to a human, 6 of the 13 clips identified; measurements on a real bank of frames, not estimates, and pinned to a date because the bank grows), and the review that sends work back, which it does often. One example: the mitigation first specified for the self-review guard would have failed open on precisely the case it was written for, and Zenbit's review caught it, not the drafting. Protocol details in the spec, meaning the x402 header names, the anchoring call's behaviour on a repeat and the contract version deployed on the testnet, were confirmed against live networks and primary sources rather than model recall, and two of the three contradicted what the model first asserted.

## File-level attribution

Kept current. Files appear here as they land.

| File | How it came to be |
|---|---|
| `README.md` | Drafted by the model from `docs/spec/01-agent-layer.md`; structure and claims reviewed and edited by Zenbit |
| `DISCLOSURE.md` | Drafted by the model; every factual claim (commit counts, dates, what existed before the event) verified against the repository by Zenbit |
| `AI-USAGE.md` | This file. Drafted by the model, rewritten by Zenbit |
| `docs/spec/README.md` | Drafted by the model; the three-part invariant rule is a Zenbit decision it was told to encode |
| `docs/spec/01-agent-layer.md` | Invariants, architecture and scope are Zenbit's; drafting and the wire-detail section are the model's, corrected against live checks |
| `app/api/agent/windows/route.ts` | Model-drafted; the ordering it exists to express, verify then work then settle, and the decision to write it as a route handler rather than a request wrapper are Zenbit's |
| `lib/x402.ts` | Model-drafted against the installed packages rather than from documentation, after the published integration turned out to describe a different major version |
| `lib/windows/types.ts`, `lib/windows/snapshot.ts`, `lib/windows/embargo.ts` | Model-drafted from `docs/spec/02-candidate-windows.md`, which Zenbit wrote first; the refuse-rather-than-filter rule and the drop-rather-than-redact rule are Zenbit decisions it was told to encode |
| `lib/agent/pay.ts` | Model-drafted; reviewed by Zenbit, which removed a flag that answered two questions with one value and made the payer a parameter |
| `test/run.ts`, `test/facilitator.ts` | Model-drafted; the standard every check is held to, that it has been seen to fail and that a refusal is paired with a negative control, is Zenbit's and predates the file |
| `fixtures/windows.synthetic.jsonl`, `fixtures/README.md` | Model-generated synthetic data. No real window, no real video id, and deliberately not the calibration span, which contains an operator's hands |
| `lib/agent/propose.ts`, `lib/agent/ledger.ts`, `lib/agent/ens.ts` | Model-drafted after reading the receiving route rather than from documentation; keying the ledger on the window id alone, and writing it before the call returns, are Zenbit decisions carried from the rejection semantics that route already had |
| `bin/agent.ts` | Model-drafted; that it has no confirmation path is inherited from the credential design rather than decided here |
| `test/ingest.ts` | Model-drafted; every refusal shape was copied from the receiving route, which lives in a repository this one cannot change, so the copy is deliberate and kept in one place |
| `docs/spec/03-proposal.md` | Model-drafted from the code, which is the reverse of the order the other two specs were written in, and the document says so |
| `bin/probe.ts` | Model-drafted; reading the recipient from the live challenge rather than the environment, and refusing when it is unreadable, are Zenbit review decisions |
| `app/layout.tsx`, `next.config.mjs`, `tsconfig.json`, `package.json`, `.env.example` | Framework scaffolding, model-written to the minimum the app needs; the dependency pins and the prose in `.env.example` are Zenbit's |
| `.github/workflows/ci.yml` | Model-drafted from a specification Zenbit wrote, including the sha pins, the node major and the decision to omit lint; the action shas were resolved against the GitHub API rather than copied |
| `LICENSE`, `.gitignore` | Generated by GitHub at repository creation; the copyright legend was set by Zenbit's legal review |
| `CONTRIBUTING.md` | The Developer Certificate of Origin 1.1, verbatim, with a two-sentence preamble from Zenbit's legal review; no model text |
| `upstream/xovi-changes/` | Adapted from the pre-existing project: copied from Xovi's private repository at origin/dev 2754a0eb on 2026-09-08, with the redactions named in the directory README made by Zenbit's legal review; the directory README was model-drafted and reviewed by Zenbit |

Code files are added as they are written, naming which are model-drafted, which are hand-written, and which are adapted from the pre-existing project.

## Commit trailer

Commits made with model assistance carry `Assisted-by: Claude Code`. No session URL, because the tooling's default trailer points at a private session link, which would be a dead end for anyone reading this history.
